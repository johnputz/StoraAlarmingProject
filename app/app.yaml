# Stora Resource Health Monitoring Dashboard
# Databricks App configuration
# Data is bundled as CSV files - no SQL warehouse or UC permissions required.

command:
  - streamlit
  - run
  - app.py
  - --server.port
  - "8000"
